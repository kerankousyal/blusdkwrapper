repositories{
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile(name: 'app', ext: 'aar')
    implementation 'com.google.code.gson:gson:2.8.2'
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
